__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.eggs/
*.egg
.pytest_cache/
.coverage

# Generated outputs — recreated by lankit generate/apply/rollback-card/etc.
ansible/generated/
network.png
rollback-card.txt
password-card.txt

# Live WiFi vault — contains real passphrases encrypted with ansible-vault.
# Create yours with: lankit secrets
# See wifi-vault.example.yml for the expected structure.
wifi-vault.yml

# Your private network declaration — contains real IPs, MACs, hostnames.
# Copy network.yml.dist to network.yml and fill in your values.
network.yml

# Local Claude Code settings — permission grants are instance-specific.
.claude/settings.local.json
certs/
# TLS certificates — generated by mkcert, never committed.
